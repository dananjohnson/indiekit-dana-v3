{
  "name": "Indiekit server for dana-v3 website",
  "keywords": ["indieweb", "micropub"],
  "success_url": "/",
  "env": {
    "GITHUB_TOKEN": {
      "description": "A GitHub personal access token",
      "required": true
    },
    "GITHUB_USER": {
      "description": "Username on GitHub",
      "required": true
    },
    "GITHUB_REPO": {
      "description": "Name of the repository files will be saved to",
      "required": true
    },
    "GITHUB_BRANCH": {
      "description": "Name of the repository branch to push to",
      "required": true
    },
    "PUBLICATION_URL": {
      "description": "URL of the website you want to publish to",
      "required": true
    }
  }
}
